
export enum RoleTitle {
  ADMIN = "Admin",
  CUSTOMER = "Customer",
}

export enum RoleId {
  ADMIN = 1,
  CUSTOMER = 2,
}

export enum AccountType{
  CHECKING = "CHECKING",
  SAVING = "SAVING",
}

export enum AccountInterest{
  CHECKING = 0.01,
  SAVING = 0.01,
}
